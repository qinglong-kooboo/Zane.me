{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "launch nuxt",
      "protocol": "inspector",
      "program": "${workspaceRoot}/node_modules/.bin/nuxt"
    }
  ]
}
